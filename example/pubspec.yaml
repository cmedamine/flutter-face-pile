name: example
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_random_user: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_face_pile:
    path: "../"

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
