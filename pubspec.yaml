name: flutter_face_pile
description: A group of overlapping round avatars are called face piles, a face pile is a series of overlapping avatar images that come and go as users join and leave a given group.
issue_tracker: https://github.com/cmedamine/flutter-face-pile/issues
repository: https://github.com/cmedamine/flutter-face-pile
version: 0.0.3

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  transparent_image: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
